.content
  .page-header
    .options.pull-right
      = link_to text_with_icon('plus', 'Add a domain'), new_domain_path, :class => 'btn primary'
    %h1 Domains
  .row
    .span12
      %table.table.table-striped.table-bordered{:id => 'domains'}
        %thead
          %th Name
          %th Catch all
          %th{:style => 'width:180px'}
        %tbody
        - @domains.each do |domain|
          %tr
            %td= domain.name
            %td= domain.catch_all
            %td
              .pull-right
                = link_to text_with_icon('cog', 'Edit'), domain_path(domain.name), :class => 'btn'
                = link_to text_with_icon('trash', 'Delete'), domain_path(domain.name), :method => 'DELETE', :confirm => 'Are you sure you wish to delete this domain?', :class => 'btn'


