.options{:style => 'text-align:right; margin-bottom:20px'}
  = link_to text_with_icon('plus', 'Add a mapping'), new_manage_domain_mapping_path(@domain), :class => 'btn'
%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Source Path
      %th Target
      %th{:style => 'width: 180px'}
  %tbody
    - @mappings.each do |mapping|
      %tr
        %td= mapping.source_path
        %td= mapping.target
        %td
          = link_to text_with_icon('cog', 'Edit'),  edit_manage_domain_mapping_path(@domain, mapping), :class => 'btn'
          = link_to text_with_icon('trash', 'Delete'), manage_domain_mapping_path(@domain, mapping), :method => 'DELETE', :confirm => 'Are you sure you wish to delete this mapping?', :class => 'btn'